# chrome-bridge 環境設定テンプレート
# このファイルをコピーして .env として使用してください:
#   cp .env.example .env

# WSL2環境の場合、Windows側のIPアドレスを設定
# 取得方法: cat /etc/resolv.conf | grep nameserver | awk '{print $2}'
# WINDOWS_HOST=172.x.x.x

# サーバーバインドアドレス
# 0.0.0.0: 全インターフェース（WSL2でWindows側からアクセスする場合に必要）
# 127.0.0.1: ローカルのみ（セキュリティ重視）
# HOST=0.0.0.0

# ポート設定
# BRIDGE_PORT=8765
# CC_BRIDGE_PORT=8766
# CDP_PORT=9223
